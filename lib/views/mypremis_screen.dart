import 'package:flutter/material.dart';

class MyPremis extends StatelessWidget {
  const MyPremis({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MyPremis', style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
